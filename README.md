# An example of using python twisted

## basic instructions:

1. install python dev tools. ubuntu: `sudo apt-get install python-dev`
2. install pip and make a virtualenv
3. install required libs: `pip install -r requirements.txt`.

